{
  "name": "databiomics-studio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "echo 'Build each service individually'",
    "test": "pnpm -r --if-present test"
  }
}
